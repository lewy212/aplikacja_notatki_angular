import { CategoryPrintDirective } from './category-print.directive';

describe('CategoryPrintDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryPrintDirective();
    expect(directive).toBeTruthy();
  });
});
